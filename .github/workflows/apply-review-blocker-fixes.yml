name: Apply review blocker fixes

on:
  push:
    branches: [fix/foundational-contracts-20260831]
    paths:
      - '.github/workflows/apply-review-blocker-fixes.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/foundational-contracts-20260831
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply guarded review fixes
        run: python scripts/apply-review-blocker-fixes.py
      - name: Format Rust
        run: cargo fmt --manifest-path src-tauri/Cargo.toml
      - name: Validate frontend and diff
        run: |
          git diff --check
          pnpm install --frozen-lockfile
          pnpm typecheck
          pnpm lint
          pnpm test
          pnpm validate:windows-bundle
          pnpm build
      - name: Remove one-shot tooling and commit
        run: |
          git rm -f scripts/apply-review-blocker-fixes.py .github/workflows/apply-review-blocker-fixes.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'fix: close remaining review safety blockers'
          git push origin HEAD:fix/foundational-contracts-20260831
