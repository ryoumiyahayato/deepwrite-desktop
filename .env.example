# DeepWrite intentionally does not accept DeepSeek API keys through environment files.
# Configure the key inside: 设置 → AI → DeepSeek.
# It is stored in the local Tauri Stronghold vault and never committed to this repository.
